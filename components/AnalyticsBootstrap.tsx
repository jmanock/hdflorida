"use client";
import { useEffect, useState, useSyncExternalStore } from "react";
import { analyticsDecision, initializeAnalytics, subscribeAnalyticsDebug, resetAnalyticsDebug, type AnalyticsDebugRecord } from "@/lib/analytics";
export function AnalyticsBootstrap(){const [records,setRecords]=useState<AnalyticsDebugRecord[]>([]),debug=useSyncExternalStore(()=>()=>{},()=>analyticsDecision().debug,()=>false);useEffect(()=>{initializeAnalytics("G-MRBXKRDT4G","x2rr9176m5");return subscribeAnalyticsDebug(setRecords)},[]);if(!debug)return null;return <aside aria-label="Analytics debug" style={{position:"fixed",right:8,bottom:8,zIndex:9999,maxWidth:420,maxHeight:260,overflow:"auto",background:"#102a43",color:"white",padding:12,font:"12px/1.4 monospace",borderRadius:8}}><strong>FDN analytics debug</strong><button style={{float:"right"}} type="button" onClick={resetAnalyticsDebug}>Reset</button>{records.slice(-12).map((x,i)=><pre key={`${x.timestamp}-${i}`} style={{whiteSpace:"pre-wrap"}}>{JSON.stringify(x,null,2)}</pre>)}</aside>}
